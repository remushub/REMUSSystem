# create docker volume
docker volume create node_modules

# use dummy container to copy node_modules into the docker volume

docker container --name temp -v node_modules:/data busybox
docker cp . temp:/data
docker rm temp

# node_module docker volume can now be mounted onto angular app container.

docker run -v node_modules -p 4200:4200 image/name
